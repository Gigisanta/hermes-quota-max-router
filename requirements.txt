# Core
litellm[proxy]>=1.55.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pyyaml>=6.0.1
httpx>=0.27.0
python-dotenv>=1.0.1

# Registry / Storage
sqlmodel>=0.0.22
redis>=5.2.0

# Vector search (Qdrant client — local mode for dev)
qdrant-client>=1.12.0

# Orchestrator
jinja2>=3.1.4

# Dashboard
gradio>=5.0.0

# Observability
langsmith>=0.1.0
prometheus-client>=0.21.0

# Redis fallback when no server is available (also used by tests)
fakeredis>=2.26.0
